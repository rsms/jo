#!/bin/sh
set -e

pushd "$(dirname "$0")" >/dev/null
JOBINDIR=$(pwd)
JOLIBDIR=$JOBINDIR/.jo
JOROOT=$(dirname "$JOBINDIR")
popd >/dev/null

export NODE_PATH="${JOROOT}/jo/node_modules"

exec -a "$0" node --harmony --stack-trace-limit=25 "$JOLIBDIR/index.js" "$@"
