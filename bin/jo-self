#!/bin/sh
set -e

pushd "$(dirname "$0")" >/dev/null
JOBINDIR=$(pwd)
JOLIBDIR=$JOBINDIR/.build
JOROOT=$(dirname "$JOBINDIR")
popd >/dev/null

export NODE_PATH="${JOLIBDIR}:${JOROOT}/node_modules"

exec -a "$0" node --harmony --stack-trace-limit=25 "$JOLIBDIR/jo/jo/index.js" "$@"
